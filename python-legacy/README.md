# python-legacy

Frozen reference implementation of the original Python Textual TUI.

Source of truth for Phase 3b engine bug fixes (see `tasks/todo.md`). This
directory should not be edited — the Rust port in `src-tauri/` is the
production codebase.

Delete this directory in a future cleanup once the Phase 3b bugs are fixed
and re-validated in Rust.
